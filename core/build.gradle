apply plugin: EnterprisePlugin

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web:2.1.18.RELEASE'
    implementation 'org.springframework:spring-core:5.2.0.RELEASE'
}

enterprise {
    environment = 'staging'
    strictMode = true
}