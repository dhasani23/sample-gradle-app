# Enterprise Gradle Configuration
org.gradle.jvmargs=-Xmx512m -Dfile.encoding=UTF-8
org.gradle.parallel=false
org.gradle.daemon=false
org.gradle.configureondemand=false

# Enterprise repository settings
repoUser=enterprise-user
repoPassword=enterprise-password
internalRepoUrl=http://internal-repo.company.com

# Java 8 specific settings
sourceCompatibility=1.8
targetCompatibility=1.8

# Security settings (commented out for compatibility)
# java.security.manager=default
# java.security.policy=enterprise.policy